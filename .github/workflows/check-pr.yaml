name: Test, Build & Merge

on:
  push:
    branches:
      - malou

jobs:
  deploy:
    name: Test, Build & Merge
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install 🏗️
        run: npm install

      - name: Test 🧪
        run: npm run test

      - name: Build 🛠️
        run: npm run build

      - name: Merge 🔀
        if: success()
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"
          git fetch
          git checkout main
          git merge --no-ff malou
          git push origin main
